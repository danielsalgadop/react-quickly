ReactDOM.render(
  React.createElement('h1', null, 'In script Text, to avoid "Target container is not a DOM element" DOM element be defined before this script'),
  document.getElementById('in_script')
)
